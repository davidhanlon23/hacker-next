import Document, { Head, Main, NextScript } from "next/document";

export default class MyDocument (){
    return(
        <html lang="en-US">
            <Head>
            
            </Head>
        
        </html>
    );
}